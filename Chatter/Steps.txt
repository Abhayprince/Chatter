Create a git repo Chatter

Create MVC Project with name  Chatter

Create new  branch from Visual Studio -		Initial-commit
Commit and push the project to this new branch

git pull origin master --allow-unrelated-histories

Resolve conflicts if any

Commit and push that changes

Create a Pull Request & Merge this Pull Request to Master

Create new  branch from Visual Studio -		Initial-commit
Commit and push the project to this new branch

git pull origin master --allow-unrelated-histories

Create a Pull Request & Merge this Pull Request to Master

